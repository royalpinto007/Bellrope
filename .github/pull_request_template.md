## What this changes

<!-- One or two lines. -->

## Why

<!-- The problem it solves. Link an issue if there is one. -->

## Checklist

- [ ] `npm run typecheck`, `npm test` and `npm run format:check` pass
- [ ] `npm run build` succeeds and `npm run test:e2e` passes
- [ ] No new network requests beyond fetching a watched page
- [ ] Nothing checks more often than the user asked it to
- [ ] No new permissions in `manifest.json`
